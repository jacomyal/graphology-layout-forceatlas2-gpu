import { default as Graph } from '../../@types/graphology';

export declare function getClustersGraph(order: number, size: number, clustersCount: number, clusterDensity: number, seed?: number): Graph;
